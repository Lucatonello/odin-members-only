<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>
    <h1>Home</h1>
    <h2>See what's happening...</h2>

    <% if (authenticated) { %>
        <form action="/new-storie" method="POST">
            <label for="newPost">Create new message</label>
            <input type="text" name="newMessage">
            <button type="submit">Post</button>
        </form>
    <% } %>
    <hr>
    <h1>Stories</h1>
    <ul>
        <% for (let i = 0; i < stories.rows.length; i++) { %>
            <li>
                <%= stories.rows[i].text %>
            </li>
        <% } %>
    </ul>
    <% if (authenticated) { %>
        <form action="/home" method="POST">
            <label for="passcode">Want to join the club?</label>
            <input type="text" name="passcode" placeholder="Enter the secret passcode">
            <button type="submit">Send</button>
        </form>
    <% } %>
</body>
</html>